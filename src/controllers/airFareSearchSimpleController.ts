/**
 * ASB APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  ContentTypeEnum,
  contentTypeEnumSchema,
} from '../models/contentTypeEnum.js';
import {
  HotelscancelReservationRequest,
  hotelscancelReservationRequestSchema,
} from '../models/hotelscancelReservationRequest.js';
import { string } from '../schema.js';
import { BaseController } from './baseController.js';

export class AirFareSearchSimpleController extends BaseController {
  /**
   * @param authorization
   * @param contentType
   * @return Response from the API call
   */
  async init(
    authorization: string,
    contentType: ContentTypeEnum,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('GET', '/1');
    const mapped = req.prepareArgs({
      authorization: [authorization, string()],
      contentType: [contentType, contentTypeEnumSchema],
    });
    req.header('Authorization', mapped.authorization);
    req.header('Content-Type', mapped.contentType);
    req.authenticate(false);
    return req.call(requestOptions);
  }

  /**
   * @param authorization
   * @param body
   * @return Response from the API call
   */
  async fareSearch(
    authorization: string,
    body: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('POST', '/air/fare_search');
    const mapped = req.prepareArgs({
      authorization: [authorization, string()],
      body: [body, string()],
    });
    req.header('Authorization', mapped.authorization);
    req.header('Content-Type', 'application/json');
    req.text(mapped.body);
    req.authenticate(false);
    return req.call(requestOptions);
  }

  /**
   * @param authorization
   * @param body
   * @return Response from the API call
   */
  async pricing(
    authorization: string,
    body: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('POST', '/air/pricing');
    const mapped = req.prepareArgs({
      authorization: [authorization, string()],
      body: [body, string()],
    });
    req.header('Authorization', mapped.authorization);
    req.header('Content-Type', 'application/json');
    req.text(mapped.body);
    req.authenticate(false);
    return req.call(requestOptions);
  }

  /**
   * @param authorization
   * @param body
   * @return Response from the API call
   */
  async book(
    authorization: string,
    body: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('POST', '/air/book');
    const mapped = req.prepareArgs({
      authorization: [authorization, string()],
      body: [body, string()],
    });
    req.header('Authorization', mapped.authorization);
    req.header('Content-Type', 'application/json');
    req.text(mapped.body);
    req.authenticate(false);
    return req.call(requestOptions);
  }

  /**
   * @return Response from the API call
   */
  async wait10second(
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('GET', '/10');
    return req.call(requestOptions);
  }

  /**
   * @param apiKey
   * @param timestamp
   * @param nonce
   * @param locale
   * @param customerIp
   * @param sessionId
   * @param userAgent
   * @param system
   * @param bookingUid
   * @param currency
   * @param authorization
   * @return Response from the API call
   */
  async getReservationDetails(
    apiKey: string,
    timestamp: string,
    nonce: string,
    locale: string,
    customerIp: string,
    sessionId: string,
    userAgent: string,
    system: string,
    bookingUid: string,
    currency: string,
    authorization: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('GET', '/air/get_reservation_details');
    const mapped = req.prepareArgs({
      apiKey: [apiKey, string()],
      timestamp: [timestamp, string()],
      nonce: [nonce, string()],
      locale: [locale, string()],
      customerIp: [customerIp, string()],
      sessionId: [sessionId, string()],
      userAgent: [userAgent, string()],
      system: [system, string()],
      bookingUid: [bookingUid, string()],
      currency: [currency, string()],
      authorization: [authorization, string()],
    });
    req.header('Authorization', mapped.authorization);
    req.query('api_key', mapped.apiKey);
    req.query('timestamp', mapped.timestamp);
    req.query('nonce', mapped.nonce);
    req.query('locale', mapped.locale);
    req.query('customer_ip', mapped.customerIp);
    req.query('session_id', mapped.sessionId);
    req.query('user_agent', mapped.userAgent);
    req.query('system', mapped.system);
    req.query('booking_uid', mapped.bookingUid);
    req.query('currency', mapped.currency);
    req.authenticate(false);
    return req.call(requestOptions);
  }

  /**
   * @param authorization
   * @param body
   * @return Response from the API call
   */
  async cancelReservation(
    authorization: string,
    body: HotelscancelReservationRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('POST', '/air/cancel_reservation');
    const mapped = req.prepareArgs({
      authorization: [authorization, string()],
      body: [body, hotelscancelReservationRequestSchema],
    });
    req.header('Authorization', mapped.authorization);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate(false);
    return req.call(requestOptions);
  }
}
